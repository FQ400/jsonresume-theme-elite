{{#if resume.basics.summary}}
    <section class="section-summary">
        {{> components/title icon='paperclip' title='Zusammenfassung'}}
        <div class="p-3">
            {{{resume.basics.summary}}}
        </div>
    </section>
{{/if}}
